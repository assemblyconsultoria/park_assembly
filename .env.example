# PostgreSQL Database Configuration
# These variables configure the PostgreSQL container
POSTGRES_DB=park_assembly
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Notes:
# - PostgreSQL 16 will run as a Docker container (no need for local installation)
# - Database and tables are created automatically
# - Default admin user is created with username: 'admin' and password: 'admin'
# - Change these credentials in production!
# - Data is persisted in Docker volume 'postgres_data'
